{
  "name": "nodle-rollup",
  "private": true,
  "workspaces": [
    "contracts",
    "graph",
    "packages/*"
  ],
  "scripts": {
    "lint": "yarn workspace contracts lint",
    "test": "yarn workspace contracts test",
    "fmt": "yarn workspace contracts fmt:check",
    "bootstrap-graph": "yarn workspace graph fix-paths && yarn workspace graph codegen"
  }
}