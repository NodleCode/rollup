#!/usr/bin/expect -f

set timeout -1
spawn zksync-cli dev start
match_max 100000
expect -re {.*Node type to use.*}
send -- "\033\[B"
send -- "\r"
expect -re {.*Additional modules to use.*}
send -- " "
send -- "\033\[B"
send -- " "
send -- "\r"
expect eof
